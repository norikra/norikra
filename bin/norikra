#!/bin/bash

binpath=$(dirname $0)/norikra-server

options="-J-server -J-XX:-UseGCOverheadLimit"

while getopts "X:" opts
do
    case $opts in
	X)
	    options=$options" -J-X"$OPTARG
	    ;;
    esac
done
shift `expr $OPTIND - 1`

exec jruby $options $binpath $@
